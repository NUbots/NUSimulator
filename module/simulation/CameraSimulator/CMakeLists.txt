# Build our NUClear module
FIND_PACKAGE(OGRE REQUIRED)
FIND_PACKAGE(OIS REQUIRED)
NUCLEAR_MODULE(INCLUDE
	${OGRE_INCLUDE_DIRS} 
	${OIS_INCLUDE_DIR}
	${OGRE_Overlay_INCLUDE_DIRS}
LIBRARIES 
	${OGRE_LIBRARIES} 
	${OIS_LIBRARIES} 
	${OGRE_Overlay_LIBRARIES}
)